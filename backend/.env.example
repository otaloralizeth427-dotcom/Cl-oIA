SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
OPENAI_API_KEY=
SUPABASE_STORAGE_BUCKET=library

# Nombre de la tabla donde se guardan los chunks con su embedding.
SUPABASE_CHUNKS_TABLE=document_chunks

# Cadena de conexión directa a Postgres (Project Settings → Database →
# Connection string). Opcional: solo la usa scripts/setup_supabase.py para
# verificar que la extensión "vector" esté activa, ya que eso no se puede
# consultar a través de la API REST de Supabase.
SUPABASE_DB_URL=
